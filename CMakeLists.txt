# include the main directory here at root in order to allow in-place development
# this way you can just clone the repo into your /src including all the off-topic
# files like git hooks, clang configurations, ...
add_subdirectory(fmi_adapter)